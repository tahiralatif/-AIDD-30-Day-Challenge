# Contracts

This directory contains API contracts (e.g., OpenAPI, GraphQL schemas) for the `calculator-app` feature.

For this specific feature, as it is a command-line interface (CLI) application with no external API exposure, no explicit API contracts are generated.
